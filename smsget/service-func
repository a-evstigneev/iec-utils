logmsg()
{
	local msg; msg=$1
	printf '\t%s\n' "$msg" 
}

logmsgtime()
{
	local msg pid; msg=$1
	pid=$(echo $$)
	printf '%s\n' "$(LANG=en date '+%b %d %T') $(cat /etc/hostname) $(basename "$0")[${pid}]: $msg"
}
