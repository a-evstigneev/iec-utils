logmsg()
{
	local msg 
	
	msg=$1
	/usr/bin/printf '%s\n' "$msg" >&2 
}

logmsgtime()
{
	local msg pid
	
	msg=$1
	pid=$(echo $$)
	/usr/bin/printf '%s\n' "$(LANG=en date '+%b %d %T') $(cat /etc/hostname) $(basename "$0")[${pid}]: $msg" >&2
}
